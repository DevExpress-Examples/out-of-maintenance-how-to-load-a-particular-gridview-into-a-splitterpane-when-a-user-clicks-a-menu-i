@model CS.Model.MyViewModel
@{
    ViewBag.Title = "GridViewAction";
}
@{if (ViewData["GridViewName"].ToString() == "products") {
    @Html.Partial("GridViewPartialProducts", Model.Products)
  } else {
    @Html.Partial("GridViewPartialCustomers", Model.Customers);
  }
}
